import 'package:flutter/material.dart';

class NiHao extends StatelessWidget {
  const NiHao({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
